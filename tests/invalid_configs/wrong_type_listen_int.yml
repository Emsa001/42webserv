# Wrong data type: listen should be STRING not INT
server0:
    server_name: "MainServer"
    listen: 80
    max_client_body_size: 4096
    max_client_header_size: 1024
    keep_alive: 30

    location:
        path: "/"
        root: "/mainserver/"
        methods: "GET"
