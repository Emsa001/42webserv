# Missing required server_name field
server0:
    listen: "80"
    max_client_body_size: 4096
    max_client_header_size: 1024
    keep_alive: 30

    location:
        path: "/"
        root: "/mainserver/"
        methods: "GET"
