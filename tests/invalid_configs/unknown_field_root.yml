# Unknown field 'bad_root_field' in root block
log_format: "[server] | 📅 [date] [time] | 🔄 [[method]] [uri] | [status] ([reason]) | 📦 [size]B"
bad_root_field: "invalid"

server0:
    server_name: "MainServer"
    listen: "80"
    max_client_body_size: 4096
    max_client_header_size: 1024
    keep_alive: 30

    location:
        path: "/"
        root: "/mainserver/"
        methods: "GET"
