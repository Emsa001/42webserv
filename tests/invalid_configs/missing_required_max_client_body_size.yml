# Missing required max_client_body_size field
server0:
    server_name: "MainServer"
    listen: "80"
    keep_alive: 30

    location:
        path: "/"
        root: "/mainserver/"
        methods: "GET"
